//import { DH } from "./scripts/config/config.js";
//import { DHplayerCharacterSheet } from "./scripts/sheets/actors/dh-pc.js";


Hooks.once("init", () => {
    console.log("Daggerheart | Initializing")
    //CONFIG.DH = DH;

    /*
    Items.unregisterSheet("core", ItemSheet);
    Items.registerSheet("dh", DHItemSheet, { makeDefault: true, label: "ItemSheet" });
    */

    //Actors.unregisterSheet("core", ActorSheet);
    //Actors.registerSheet("dh", DHplayerCharacterSheet, { makeDefault: true, label: "Actor Sheet" });

    //loadTemplates(CONFIG.DH.TEMPLATES);
});
